{
  "name": "piccped",
  "version": "0.0.1",
  "description": "PWA with Preact for Piccped",
  "repository": "git@gitlab.com:felipe6/piccped.git",
  "main": "src/index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.2.0",
    "eslint-config-synacor": "^1.0.1",
    "if-env": "^1.0.0",
    "preact-cli": "^1.3.0"
  },
  "dependencies": {
    "font-awesome": "^4.7.0",
    "preact": "^8.1.0",
    "preact-compat": "^3.16.0",
    "preact-redux": "^2.0.3",
    "preact-router": "^2.5.4",
    "react-audio-player": "^0.6.2",
    "react-helmet": "^5.2.0",
    "react-html5video": "^2.3.0",
    "react-slick": "^0.14.11",
    "react-transition-group": "^2.1.0",
    "redux": "^3.7.2",
    "slick-carousel": "^1.6.0"
  }
}
